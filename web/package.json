{
  "name": "loominary-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "shots": "playwright test --config e2e/playwright.config.ts --project=shots",
    "broll": "playwright test --config e2e/playwright.config.ts --project=broll"
  },
  "dependencies": {
    "@bokuweb/zstd-wasm": "^0.0.27",
    "mp4-muxer": "^5.2.2",
    "pako": "^2.1.0",
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@preact/preset-vite": "^2.9.4",
    "@types/pako": "^2.0.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
